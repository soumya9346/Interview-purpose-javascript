// the switch case is an branching statement 
// the switch case evaluates the cases and executes one matched statement.

var a = 1;
var b = 10;
switch(a>b){
    case 1: console.log("a is bigger")
           break;
    case 2: console.log("b is smaller")
           break;
    default: console.log("b is bigger")
}