let a = 5;
if(a>3){
    console.log("this is a block scope");
    let b = 10;
}
console.log("in " +b)

// when the let and const variable are used { } and outside the curly braces 
// it cannot be accessed 

// when the var keyword is used { } and outside the curly braces can be accessed 